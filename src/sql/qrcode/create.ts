// import connection from "..";

// export default function saveQrCode(name: string, value: string) {

//   const sql = `INSERT INTO qrcode (name, value) VALUES ("${name}", "${value}")`;

//   return connection.query(
//     sql,
//     (err, _) => {

//       if (err)
//         return console.log(err);
//     },
//   );

// }